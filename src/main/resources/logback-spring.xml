<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>[%d{yyyy-MM-dd HH:mm:ss.SSS}] [mktp: ] [rid: %mdc{rid}] [cid: %mdc{cid}] [pid: %thread] [%logger{36}] %level: %msg%n</pattern>
        </encoder>
    </appender>

    <springProfile name="test">
        <logger name="org.springframework" level="info" additivity="false">
            <appender-ref ref="STDOUT" />
        </logger>

        <logger name="com.boleto.api" level="debug" additivity="false">
            <appender-ref ref="STDOUT" />
        </logger>

        <root level="error">
            <appender-ref ref="STDOUT" />
        </root>
    </springProfile>

    <springProfile name="dev">
        <timestamp key="executionDate" datePattern="yyyy-MM-dd"/>

        <appender name="FILE" class="ch.qos.logback.core.FileAppender">
            <file>./logs/app-${executionDate}.log</file>
            <append>true</append>
            <encoder>
                <pattern>[%d{yyyy-MM-dd HH:mm:ss.SSS}] [mktp: ] [rid: %mdc{rid}] [cid: %mdc{cid}] [pid: %thread] [%logger{36}] %level: %msg%n</pattern>
            </encoder>
        </appender>

        <logger name="org.springframework" level="info" additivity="false">
            <appender-ref ref="STDOUT" />
            <appender-ref ref="FILE" />
        </logger>

        <logger name="com.boleto.api" level="debug" additivity="false">
            <appender-ref ref="STDOUT" />
            <appender-ref ref="FILE" />
        </logger>

        <root level="error">
            <appender-ref ref="STDOUT" />
            <appender-ref ref="FILE" />
        </root>
    </springProfile>



</configuration>